---
"coily": minor
---

**BREAKING:** Rename `resting` to `isResting` across all APIs.

- `spring.resting` → `spring.isResting`
- `useSpring().resting` → `useSpring().isResting`
- `SpringValue` slot prop `resting` → `isResting`
